<h1>All The Guests</h1>
<p> <%= link_to("Go to Episodes", episodes_path) %> </p>

<ul>
  <% @guests.each do |guest| %>
    <li>
    	<%= link_to(guest.name, guest_path(guest)) %>
    </li>
  <% end %>
</ul>
